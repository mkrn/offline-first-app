{
  "name": "wtc-full-stack",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "workspaces": {
    "packages": ["packages/*"],
    "nohoist": ["**/cors", "**/cors/**"]
  },
  "scripts": {
    "start": "export REACT_APP_API_URL='http://localhost:4000' && export PORT=4000 && yarn workspace backend-nodejs start && yarn workspace frontend start"
  },
  "private": true
}
